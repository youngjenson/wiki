server:
  port: 80
#配置mysql数据源
spring:
  datasource:
    url: jdbc:mysql://110.42.99.174:3306/wiki?characterEncoding=UTF8&autoReconnect=true&serverTimeZone=Asia/Shanghai
    username: wiki
    password: wiki
    driver-class-name: com.mysql.cj.jdbc.Driver

#配置mybatis加载mapper.xml
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl